"use client";

import { FormEvent, useMemo, useState } from "react";
import {
  ArrowRight, Bell, BriefcaseBusiness, CalendarDays, Check, ChevronDown, ChevronRight,
  CircleHelp, Clock3, FileText, Filter, LayoutDashboard, Menu, MoreHorizontal, Plus,
  Search, Settings, ShieldCheck, SlidersHorizontal, Sparkles, Users, X
} from "lucide-react";

type Stage = "Applied" | "Screening" | "Interview" | "Technical Test" | "Offer" | "Hired" | "Rejected";

type Candidate = {
  id: number; name: string; initials: string; role: string; job: string; stage: Stage;
  score: number; experience: string; skills: string[]; applied: string; tone: string;
};

const candidates: Candidate[] = [
  { id: 1, name: "Maya Chen", initials: "MC", role: "Frontend Engineer", job: "Senior Frontend Engineer", stage: "Interview", score: 91, experience: "6 years", skills: ["React", "TypeScript", "Design systems"], applied: "Today", tone: "bg-violet-100 text-violet-700" },
  { id: 2, name: "Elliot Brooks", initials: "EB", role: "Product Designer", job: "Product Designer", stage: "Screening", score: 88, experience: "5 years", skills: ["Figma", "Research", "Prototyping"], applied: "Yesterday", tone: "bg-amber-100 text-amber-700" },
  { id: 3, name: "Priya Nair", initials: "PN", role: "Backend Engineer", job: "Backend Engineer", stage: "Technical Test", score: 84, experience: "4 years", skills: ["C#", "PostgreSQL", "Docker"], applied: "Aug 10", tone: "bg-emerald-100 text-emerald-700" },
  { id: 4, name: "Jonas Meyer", initials: "JM", role: "Frontend Engineer", job: "Senior Frontend Engineer", stage: "Applied", score: 79, experience: "3 years", skills: ["React", "Next.js", "CSS"], applied: "Aug 10", tone: "bg-sky-100 text-sky-700" },
  { id: 5, name: "Nora Williams", initials: "NW", role: "Customer Success", job: "Customer Success Manager", stage: "Offer", score: 93, experience: "7 years", skills: ["SaaS", "Onboarding", "Gainsight"], applied: "Aug 8", tone: "bg-rose-100 text-rose-700" },
  { id: 6, name: "Samir Patel", initials: "SP", role: "Backend Engineer", job: "Backend Engineer", stage: "Screening", score: 76, experience: "2 years", skills: ["Python", "FastAPI", "AWS"], applied: "Aug 7", tone: "bg-indigo-100 text-indigo-700" },
  { id: 7, name: "Avery King", initials: "AK", role: "Product Designer", job: "Product Designer", stage: "Interview", score: 82, experience: "5 years", skills: ["UX", "Analytics", "Figma"], applied: "Aug 6", tone: "bg-orange-100 text-orange-700" },
];

const stages: Stage[] = ["Applied", "Screening", "Interview", "Technical Test", "Offer", "Hired"];
const nav = [
  ["Dashboard", LayoutDashboard], ["Candidates", Users], ["Jobs", BriefcaseBusiness], ["Interviews", CalendarDays],
];

function Avatar({ candidate, size = "md" }: { candidate: Candidate; size?: "sm" | "md" }) {
  return <span className={`${candidate.tone} ${size === "sm" ? "h-8 w-8 text-[10px]" : "h-10 w-10 text-xs"} inline-flex shrink-0 items-center justify-center rounded-full font-semibold`}>{candidate.initials}</span>;
}

function Score({ score }: { score: number }) {
  const color = score >= 85 ? "bg-emerald-500" : score >= 75 ? "bg-amber-400" : "bg-slate-300";
  return <span className="inline-flex items-center gap-2 text-sm font-semibold text-slate-700"><span className="h-1.5 w-1.5 rounded-full bg-brand" />{score}%</span>;
}

function Metric({ title, value, trend, icon: Icon }: { title: string; value: string; trend: string; icon: typeof Users }) {
  return <article className="surface p-5"><div className="flex items-start justify-between"><span className="eyebrow">{title}</span><span className="grid h-9 w-9 place-items-center rounded-xl bg-slate-50 text-slate-500"><Icon size={18} /></span></div><p className="mt-5 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-slate-500"><span className="font-semibold text-emerald-600">{trend}</span> from last month</p></article>;
}

export default function HireFlowApp() {
  const [view, setView] = useState("Dashboard");
  const [mobileOpen, setMobileOpen] = useState(false);
  const [search, setSearch] = useState("");
  const [selected, setSelected] = useState<Candidate | null>(null);
  const [notice, setNotice] = useState("");
  const [jobModal, setJobModal] = useState(false);
  const [formError, setFormError] = useState("");
  const [jobCreated, setJobCreated] = useState(false);
  const [candidateState, setCandidateState] = useState(candidates);

  const filtered = useMemo(() => candidateState.filter((c) => `${c.name} ${c.role} ${c.skills.join(" ")}`.toLowerCase().includes(search.toLowerCase())), [search, candidateState]);
  const showNotice = (message: string) => { setNotice(message); window.setTimeout(() => setNotice(""), 3200); };
  const moveCandidate = (candidate: Candidate) => {
    const index = stages.indexOf(candidate.stage);
    const next = stages[Math.min(index + 1, stages.length - 1)];
    if (next === candidate.stage) return showNotice(`${candidate.name} is already in the final visible stage.`);
    setCandidateState((all) => all.map((item) => item.id === candidate.id ? { ...item, stage: next } : item));
    showNotice(`${candidate.name} moved to ${next}. Activity saved.`);
  };
  const submitJob = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const data = new FormData(event.currentTarget);
    if (!data.get("title") || !data.get("department")) return setFormError("Add a job title and department to continue.");
    setFormError(""); setJobModal(false); setJobCreated(true); showNotice("Job saved as a draft. You can publish it when it is ready.");
  };

  return <main className="min-h-screen bg-mist">
    <aside className="fixed inset-y-0 left-0 z-40 hidden w-64 border-r border-line bg-white px-4 py-5 lg:block">
      <Brand />
      <p className="mt-10 px-3 eyebrow">Workspace</p>
      <nav className="mt-3 space-y-1">{nav.map(([label, Icon]) => <button key={label as string} onClick={() => setView(label as string)} className={`nav-item w-full ${(view === label) ? "nav-item-active" : ""}`}><Icon size={18} />{label as string}</button>)}</nav>
      <p className="mt-8 px-3 eyebrow">Organization</p>
      <nav className="mt-3 space-y-1"><button className="nav-item w-full"><Users size={18} />Team</button><button className="nav-item w-full"><Settings size={18} />Settings</button></nav>
      <div className="absolute bottom-5 left-4 right-4 rounded-xl bg-slate-50 p-3"><div className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-full bg-ink text-xs font-semibold text-white">MR</span><div className="min-w-0 text-left"><p className="truncate text-sm font-semibold">Morgan Reed</p><p className="truncate text-xs text-slate-500">Talent team</p></div><MoreHorizontal className="ml-auto text-slate-400" size={17} /></div></div>
    </aside>

    {mobileOpen && <div className="fixed inset-0 z-50 bg-ink/25 lg:hidden" onClick={() => setMobileOpen(false)}><aside className="h-full w-72 bg-white p-5" onClick={(e) => e.stopPropagation()}><div className="flex items-center justify-between"><Brand /><button aria-label="Close navigation" onClick={() => setMobileOpen(false)}><X /></button></div><nav className="mt-10 space-y-1">{nav.map(([label, Icon]) => <button key={label as string} onClick={() => { setView(label as string); setMobileOpen(false); }} className={`nav-item w-full ${(view === label) ? "nav-item-active" : ""}`}><Icon size={18} />{label as string}</button>)}</nav></aside></div>}

    <section className="lg:pl-64">
      <header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-line bg-white/95 px-5 backdrop-blur lg:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" aria-label="Open navigation" onClick={() => setMobileOpen(true)}><Menu /></button><div className="hidden items-center gap-2 text-sm text-slate-500 md:flex"><span>Acme Studio</span><ChevronRight size={15}/><span className="font-medium text-ink">{view}</span></div></div><div className="flex items-center gap-3"><button className="relative grid h-10 w-10 place-items-center rounded-xl border bg-white text-slate-500 hover:bg-slate-50" aria-label="Notifications"><Bell size={18}/><i className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-brand" /></button><button className="hidden h-10 items-center gap-2 rounded-xl border bg-white px-3 text-sm font-semibold md:flex"><CircleHelp size={17} className="text-slate-500"/>Help</button><button onClick={() => setJobModal(true)} className="inline-flex h-10 items-center gap-2 rounded-xl bg-brand px-3.5 text-sm font-semibold text-white hover:bg-blue-700"><Plus size={17}/>New job</button></div></header>
      <div className="mx-auto max-w-[1480px] px-5 py-7 lg:px-8">
        {view === "Dashboard" && <Dashboard candidates={candidateState} setSelected={setSelected} setView={setView} />}
        {view === "Candidates" && <CandidatesPage candidates={filtered} search={search} setSearch={setSearch} setSelected={setSelected} moveCandidate={moveCandidate} />}
        {view === "Jobs" && <JobsPage jobCreated={jobCreated} onCreate={() => setJobModal(true)} />}
        {view === "Interviews" && <InterviewsPage setSelected={setSelected} />}
      </div>
    </section>
    {selected && <CandidateDrawer candidate={candidateState.find((candidate) => candidate.id === selected.id) ?? selected} onClose={() => setSelected(null)} onMove={moveCandidate} />}
    {jobModal && <JobModal onClose={() => setJobModal(false)} onSubmit={submitJob} error={formError} />}
    {notice && <div role="status" className="fixed bottom-5 right-5 z-[70] max-w-sm rounded-xl bg-ink px-4 py-3 text-sm font-medium text-white shadow-2xl"><Check className="mr-2 inline text-emerald-300" size={17}/>{notice}</div>}
  </main>;
}

function Brand() { return <button className="flex items-center gap-2.5 text-left"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-sm font-bold text-white">H</span><span><span className="block text-lg font-semibold tracking-tight">HireFlow</span><span className="block text-[10px] font-medium uppercase tracking-[.12em] text-slate-400">Recruiting OS</span></span></button>; }

function Dashboard({ candidates, setSelected, setView }: { candidates: Candidate[]; setSelected: (c: Candidate) => void; setView: (view: string) => void }) {
  return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Tuesday, August 13</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Good morning, Morgan.</h1><p className="mt-1 text-sm text-slate-500">Here’s the pulse of your hiring activity.</p></div><button onClick={() => setView("Candidates")} className="inline-flex items-center gap-1 text-sm font-semibold text-brand hover:underline">View candidate pipeline <ArrowRight size={16}/></button></div>
    <div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><Metric title="Total candidates" value="128" trend="12.5%" icon={Users}/><Metric title="Active jobs" value="14" trend="2 added" icon={BriefcaseBusiness}/><Metric title="Interviews this week" value="18" trend="6 today" icon={CalendarDays}/><Metric title="Applications" value="46" trend="8.1%" icon={FileText}/><Metric title="Average match" value="82%" trend="4.3%" icon={Sparkles}/></div>
    <div className="mt-6 grid gap-6 xl:grid-cols-[1.55fr_.9fr]"><section className="surface p-5 sm:p-6"><div className="flex items-start justify-between"><div><h2 className="text-base font-semibold">Recruitment pipeline</h2><p className="mt-1 text-sm text-slate-500">Active applications across your open roles.</p></div><button onClick={() => setView("Candidates")} className="rounded-lg border px-3 py-1.5 text-xs font-semibold hover:bg-slate-50">Manage pipeline</button></div><div className="mt-8 grid grid-cols-3 gap-y-7 sm:grid-cols-6">{[{l:"Applied",n:24,c:"bg-slate-400"},{l:"Screening",n:18,c:"bg-blue-500"},{l:"Interview",n:12,c:"bg-violet-500"},{l:"Test",n:8,c:"bg-amber-500"},{l:"Offer",n:3,c:"bg-emerald-500"},{l:"Hired",n:5,c:"bg-ink"}].map((item) => <div key={item.l} className="border-l border-line pl-3 first:border-l-0 first:pl-0"><p className="text-2xl font-semibold">{item.n}</p><p className="mt-1 text-xs text-slate-500">{item.l}</p><div className="mt-3 h-1 w-8 rounded-full bg-slate-100"><div className={`h-full rounded-full ${item.c}`} style={{width: `${Math.min(100,item.n*4)}%`}} /></div></div>)}</div><div className="mt-8 rounded-xl border bg-slate-50 px-4 py-3 text-sm text-slate-600"><span className="font-semibold text-ink">12 candidates</span> need a decision this week. <button onClick={() => setView("Candidates")} className="ml-1 font-semibold text-brand">Review now</button></div></section>
      <section className="surface p-5 sm:p-6"><div className="flex items-center justify-between"><div><h2 className="text-base font-semibold">Upcoming interviews</h2><p className="mt-1 text-sm text-slate-500">Next 48 hours</p></div><button onClick={() => setView("Interviews")} className="text-sm font-semibold text-brand">View all</button></div><div className="mt-5 divide-y">{[["09:30", "Maya Chen", "Technical interview", "MC"],["13:00", "Avery King", "Portfolio review", "AK"],["Tomorrow", "Elliot Brooks", "Screening call", "EB"]].map(([time,name,type,initial]) => <div className="flex items-center gap-3 py-3" key={name}><span className="w-12 text-xs font-semibold text-slate-500">{time}</span><span className="grid h-8 w-8 place-items-center rounded-full bg-slate-100 text-[10px] font-bold">{initial}</span><div className="min-w-0"><p className="text-sm font-semibold">{name}</p><p className="truncate text-xs text-slate-500">{type}</p></div><ChevronRight size={16} className="ml-auto text-slate-400"/></div>)}</div></section></div>
    <div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_1fr]"><section className="surface overflow-hidden"><div className="flex items-center justify-between px-5 pb-3 pt-5 sm:px-6"><div><h2 className="text-base font-semibold">Recent applications</h2><p className="mt-1 text-sm text-slate-500">New candidates worth a closer look.</p></div><button onClick={() => setView("Candidates")} className="text-sm font-semibold text-brand">All candidates</button></div><div className="overflow-x-auto"><table className="w-full min-w-[600px] text-left"><thead className="border-y bg-slate-50 text-[11px] font-semibold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3 sm:px-6">Candidate</th><th className="px-3 py-3">Applied for</th><th className="px-3 py-3">Match</th><th className="px-5 py-3 sm:px-6">Stage</th></tr></thead><tbody>{candidates.slice(0,4).map((c) => <tr className="cursor-pointer border-b last:border-0 hover:bg-slate-50" key={c.id} onClick={() => setSelected(c)}><td className="px-5 py-3 sm:px-6"><div className="flex items-center gap-3"><Avatar candidate={c} size="sm"/><div><p className="text-sm font-semibold">{c.name}</p><p className="text-xs text-slate-500">{c.experience} experience</p></div></div></td><td className="px-3 py-3 text-sm text-slate-600">{c.job}</td><td className="px-3 py-3"><Score score={c.score}/></td><td className="px-5 py-3 sm:px-6"><StagePill stage={c.stage}/></td></tr>)}</tbody></table></div></section>
      <section className="surface p-5 sm:p-6"><div className="flex items-start justify-between"><div><h2 className="text-base font-semibold">Top matches</h2><p className="mt-1 text-sm text-slate-500">AI-assisted analysis for active roles.</p></div><Sparkles size={19} className="text-brand"/></div><div className="mt-5 space-y-4">{[...candidates].sort((a,b) => b.score-a.score).slice(0,3).map((c) => <button onClick={() => setSelected(c)} className="flex w-full items-center gap-3 text-left" key={c.id}><Avatar candidate={c}/><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><p className="truncate text-sm font-semibold">{c.name}</p><Score score={c.score}/></div><p className="mt-0.5 truncate text-xs text-slate-500">{c.role} · {c.job}</p><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand" style={{width: `${c.score}%`}}/></div></div></button>)}</div><p className="mt-5 border-t pt-4 text-xs leading-relaxed text-slate-500"><Sparkles size={13} className="mr-1 inline text-brand"/>Scores are AI-assisted guidance. Always review the original resume and interview evidence.</p></section></div>
  </>;
}

function StagePill({ stage }: { stage: Stage }) { const styles: Record<Stage, string> = { Applied: "bg-slate-100 text-slate-600", Screening: "bg-blue-50 text-blue-700", Interview: "bg-violet-50 text-violet-700", "Technical Test": "bg-amber-50 text-amber-700", Offer: "bg-emerald-50 text-emerald-700", Hired: "bg-emerald-100 text-emerald-800", Rejected: "bg-rose-50 text-rose-700" }; return <span className={`inline-flex whitespace-nowrap rounded-md px-2 py-1 text-xs font-semibold ${styles[stage]}`}>{stage}</span>; }

function CandidatesPage({ candidates, search, setSearch, setSelected, moveCandidate }: { candidates: Candidate[]; search: string; setSearch: (s: string) => void; setSelected: (c: Candidate) => void; moveCandidate: (c: Candidate) => void }) {
  return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Talent directory</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Candidates</h1><p className="mt-1 text-sm text-slate-500">Move candidates deliberately, with the context to make each decision.</p></div><button className="inline-flex h-10 items-center justify-center gap-2 rounded-xl border bg-white px-3.5 text-sm font-semibold hover:bg-slate-50"><Plus size={17}/>Add candidate</button></div><div className="mt-7 surface overflow-hidden"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row sm:items-center sm:justify-between"><label className="relative block sm:w-80"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={17}/><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search candidates or skills" className="h-10 w-full rounded-lg border bg-white pl-9 pr-3 text-sm outline-none transition focus:border-brand focus:ring-2 focus:ring-blue-100"/></label><div className="flex gap-2"><button className="inline-flex h-10 items-center gap-2 rounded-lg border px-3 text-sm font-semibold hover:bg-slate-50"><Filter size={16}/>Filter</button><button className="inline-flex h-10 items-center gap-2 rounded-lg border px-3 text-sm font-semibold hover:bg-slate-50"><SlidersHorizontal size={16}/>Sort</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[830px] text-left"><thead className="border-b bg-slate-50 text-[11px] font-semibold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Candidate</th><th className="px-3 py-3">Applied role</th><th className="px-3 py-3">Skills</th><th className="px-3 py-3">Match</th><th className="px-3 py-3">Stage</th><th className="px-5 py-3"></th></tr></thead><tbody>{candidates.map((c) => <tr key={c.id} className="border-b last:border-0 hover:bg-slate-50"><td className="px-5 py-4"><button onClick={() => setSelected(c)} className="flex items-center gap-3 text-left"><Avatar candidate={c}/><div><p className="text-sm font-semibold">{c.name}</p><p className="mt-0.5 text-xs text-slate-500">{c.role} · {c.experience}</p></div></button></td><td className="px-3 py-4 text-sm text-slate-600">{c.job}</td><td className="px-3 py-4"><div className="flex max-w-[200px] flex-wrap gap-1">{c.skills.slice(0,2).map((skill) => <span key={skill} className="rounded bg-slate-100 px-1.5 py-0.5 text-[11px] font-medium text-slate-600">{skill}</span>)}</div></td><td className="px-3 py-4"><Score score={c.score}/></td><td className="px-3 py-4"><StagePill stage={c.stage}/></td><td className="px-5 py-4"><button onClick={() => moveCandidate(c)} className="rounded-lg border px-2.5 py-1.5 text-xs font-semibold text-slate-600 hover:border-brand hover:text-brand">Advance</button></td></tr>)}</tbody></table></div>{candidates.length === 0 && <div className="p-12 text-center"><Users className="mx-auto text-slate-300" size={32}/><p className="mt-3 font-semibold">No matching candidates</p><p className="mt-1 text-sm text-slate-500">Try a different name, role, or skill.</p></div>}</div><Pipeline candidates={candidates} setSelected={setSelected} moveCandidate={moveCandidate}/></>;
}

function Pipeline({ candidates, setSelected, moveCandidate }: { candidates: Candidate[]; setSelected: (c: Candidate) => void; moveCandidate: (c: Candidate) => void }) { return <section className="mt-6"><div className="flex items-end justify-between"><div><p className="eyebrow">At a glance</p><h2 className="mt-1 text-xl font-semibold">Recruitment pipeline</h2></div><p className="hidden text-sm text-slate-500 sm:block">Use Advance to record a stage change.</p></div><div className="mt-4 flex gap-4 overflow-x-auto pb-4">{stages.slice(0,5).map((stage) => { const inStage = candidates.filter((c) => c.stage === stage); return <div key={stage} className="w-72 shrink-0"><div className="mb-2 flex items-center justify-between px-1"><span className="text-sm font-semibold">{stage}</span><span className="rounded-full bg-slate-200 px-2 py-0.5 text-xs font-semibold text-slate-600">{inStage.length}</span></div><div className="min-h-36 space-y-2 rounded-xl border border-dashed border-slate-300 bg-slate-100/60 p-2">{inStage.map((c) => <article key={c.id} className="rounded-xl border bg-white p-3 shadow-sm"><button onClick={() => setSelected(c)} className="flex w-full items-start gap-2 text-left"><Avatar candidate={c} size="sm"/><div className="min-w-0"><p className="truncate text-sm font-semibold">{c.name}</p><p className="mt-0.5 truncate text-xs text-slate-500">{c.job}</p></div><Score score={c.score}/></button><button onClick={() => moveCandidate(c)} className="mt-3 inline-flex items-center gap-1 text-xs font-semibold text-brand">Advance <ArrowRight size={13}/></button></article>)}{inStage.length === 0 && <p className="px-2 py-8 text-center text-xs text-slate-400">No candidates here</p>}</div></div>; })}</div></section>; }

function JobsPage({ jobCreated, onCreate }: { jobCreated: boolean; onCreate: () => void }) { const jobs = [["Senior Frontend Engineer", "Engineering", "Remote · Europe", "12 candidates", "Published"],["Product Designer", "Product", "London · Hybrid", "9 candidates", "Published"],["Backend Engineer", "Engineering", "Remote · UK", "16 candidates", "Published"],["Customer Success Manager", "Customer", "New York · Hybrid", "6 candidates", "Paused"]]; return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Open positions</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Jobs</h1><p className="mt-1 text-sm text-slate-500">The source of truth for every active hiring process.</p></div><button onClick={onCreate} className="inline-flex h-10 items-center justify-center gap-2 rounded-xl bg-brand px-3.5 text-sm font-semibold text-white hover:bg-blue-700"><Plus size={17}/>Create job</button></div><section className="mt-7 surface overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left"><thead className="border-b bg-slate-50 text-[11px] font-semibold uppercase tracking-wider text-slate-500"><tr><th className="px-6 py-3">Role</th><th className="px-3 py-3">Department</th><th className="px-3 py-3">Location</th><th className="px-3 py-3">Candidates</th><th className="px-3 py-3">Status</th><th className="px-6 py-3"></th></tr></thead><tbody>{jobs.map(([role,department,location,count,status]) => <tr className="border-b last:border-0 hover:bg-slate-50" key={role}><td className="px-6 py-4"><p className="text-sm font-semibold">{role}</p><p className="mt-0.5 text-xs text-slate-500">Updated 2 days ago</p></td><td className="px-3 py-4 text-sm text-slate-600">{department}</td><td className="px-3 py-4 text-sm text-slate-600">{location}</td><td className="px-3 py-4 text-sm text-slate-600">{count}</td><td className="px-3 py-4"><span className={`rounded-md px-2 py-1 text-xs font-semibold ${status === "Published" ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}>{status}</span></td><td className="px-6 py-4"><button className="text-sm font-semibold text-brand">Manage</button></td></tr>)}{jobCreated && <tr className="border-b bg-blue-50/40"><td className="px-6 py-4"><p className="text-sm font-semibold">New position</p><p className="mt-0.5 text-xs text-slate-500">Just created</p></td><td className="px-3 py-4 text-sm text-slate-600">Unassigned</td><td className="px-3 py-4 text-sm text-slate-600">—</td><td className="px-3 py-4 text-sm text-slate-600">0 candidates</td><td className="px-3 py-4"><span className="rounded-md bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-600">Draft</span></td><td className="px-6 py-4"><button className="text-sm font-semibold text-brand">Manage</button></td></tr>}</tbody></table></div></section><section className="mt-6 grid gap-4 md:grid-cols-3"><article className="surface p-5"><p className="eyebrow">Job board</p><p className="mt-2 text-lg font-semibold">Clear, useful public listings</p><p className="mt-2 text-sm leading-relaxed text-slate-500">Every published job includes its responsibilities, hiring team, and application flow.</p></article><article className="surface p-5"><p className="eyebrow">Hiring health</p><p className="mt-2 text-lg font-semibold">7 days average time to shortlist</p><p className="mt-2 text-sm leading-relaxed text-slate-500">Candidates are prioritized with reviewable AI-assisted evidence.</p></article><article className="surface p-5"><p className="eyebrow">Next action</p><p className="mt-2 text-lg font-semibold">Publish two draft roles</p><button className="mt-3 text-sm font-semibold text-brand">Review drafts <ArrowRight className="inline" size={15}/></button></article></section></>; }

function InterviewsPage({ setSelected }: { setSelected: (candidate: Candidate) => void }) { const interviewRows = [[candidates[0], "Today, 09:30", "Technical", "Ravi Shah"],[candidates[6], "Today, 13:00", "Portfolio", "Lena Ward"],[candidates[1], "Tomorrow, 10:00", "Screening", "Morgan Reed"],[candidates[2], "Aug 15, 14:30", "Technical", "Ravi Shah"]]; return <><div><p className="eyebrow">Interview schedule</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Interviews</h1><p className="mt-1 text-sm text-slate-500">Give every interviewer the context they need, without the noise.</p></div><div className="mt-7 grid gap-6 xl:grid-cols-[1.45fr_.8fr]"><section className="surface overflow-hidden"><div className="border-b p-5 sm:p-6"><h2 className="font-semibold">Upcoming</h2><p className="mt-1 text-sm text-slate-500">Four interviews scheduled this week.</p></div><div className="divide-y">{interviewRows.map(([candidate,time,type,interviewer]) => { const c = candidate as Candidate; return <button key={`${c.id}${time}`} onClick={() => setSelected(c)} className="flex w-full items-center gap-4 px-5 py-4 text-left hover:bg-slate-50 sm:px-6"><Avatar candidate={c}/><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{c.name}</p><p className="mt-0.5 text-xs text-slate-500">{c.job}</p></div><div className="hidden text-right sm:block"><p className="text-sm font-medium">{time as string}</p><p className="mt-0.5 text-xs text-slate-500">{type as string} · {interviewer as string}</p></div><ChevronRight size={17} className="text-slate-400"/></button>; })}</div></section><section className="surface p-6"><span className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-brand"><CalendarDays size={19}/></span><h2 className="mt-5 text-lg font-semibold">Prepare an interview</h2><p className="mt-2 text-sm leading-relaxed text-slate-500">A focused briefing combines the resume, the role requirements, and the questions your team has agreed on.</p><button className="mt-5 inline-flex items-center gap-1 text-sm font-semibold text-brand">Create interview <ArrowRight size={15}/></button><div className="mt-6 border-t pt-5"><p className="eyebrow">Feedback coverage</p><p className="mt-2 text-2xl font-semibold">86%</p><p className="mt-1 text-xs text-slate-500">of completed interviews have written feedback.</p></div></section></div></>; }

function CandidateDrawer({ candidate, onClose, onMove }: { candidate: Candidate; onClose: () => void; onMove: (c: Candidate) => void }) { return <div className="fixed inset-0 z-[60] bg-ink/25" onClick={onClose}><aside className="absolute right-0 top-0 h-full w-full max-w-xl overflow-y-auto bg-white shadow-2xl" onClick={(e) => e.stopPropagation()}><div className="sticky top-0 z-10 flex items-center justify-between border-b bg-white/95 px-5 py-4 backdrop-blur"><p className="text-sm font-semibold">Candidate profile</p><button onClick={onClose} aria-label="Close profile" className="grid h-9 w-9 place-items-center rounded-lg hover:bg-slate-100"><X size={19}/></button></div><div className="p-5 sm:p-7"><div className="flex items-start gap-4"><Avatar candidate={candidate}/><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><h2 className="text-xl font-semibold">{candidate.name}</h2><StagePill stage={candidate.stage}/></div><p className="mt-1 text-sm text-slate-500">{candidate.role} · {candidate.experience} experience</p><p className="mt-2 text-sm font-medium text-slate-700">Applied for {candidate.job}</p></div></div><div className="mt-6 flex gap-2"><button onClick={() => onMove(candidate)} className="inline-flex h-10 flex-1 items-center justify-center gap-1 rounded-xl bg-brand text-sm font-semibold text-white hover:bg-blue-700">Advance stage <ArrowRight size={16}/></button><button className="inline-flex h-10 items-center justify-center rounded-xl border px-4 text-sm font-semibold hover:bg-slate-50">Add note</button></div><section className="mt-7 rounded-2xl border border-blue-100 bg-blue-50/60 p-5"><div className="flex items-center gap-2"><Sparkles size={18} className="text-brand"/><h3 className="font-semibold">AI-assisted analysis</h3><span className="ml-auto rounded bg-white px-2 py-1 text-xs font-bold text-brand">{candidate.score}% match</span></div><p className="mt-3 text-sm leading-relaxed text-slate-600">{candidate.name} shows a strong alignment with the core requirements for this role, particularly in relevant technical delivery and cross-functional collaboration.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><p className="eyebrow">Strong matches</p><div className="mt-2 flex flex-wrap gap-1.5">{candidate.skills.map((skill) => <span className="rounded-md bg-white px-2 py-1 text-xs font-semibold text-slate-700" key={skill}>{skill}</span>)}</div></div><div><p className="eyebrow">Potential gap</p><p className="mt-2 text-sm text-slate-600">Confirm experience operating at your team’s scale during the interview.</p></div></div><p className="mt-5 border-t border-blue-100 pt-3 text-xs leading-relaxed text-slate-500">AI output is guidance, not a hiring decision. Review the original resume and interview evidence before proceeding.</p></section><section className="mt-7"><div className="flex items-center justify-between"><h3 className="font-semibold">Application activity</h3><button className="text-sm font-semibold text-brand">View all</button></div><div className="mt-5 space-y-5 border-l border-slate-200 pl-5">{[["Today, 08:42","AI analysis completed","Skills and experience evaluated against the job requirements."],["Yesterday, 15:10",`Moved to ${candidate.stage}`,"Morgan Reed updated the candidate’s application stage."],["Aug 10, 09:18","Application received",`Applied for ${candidate.job}.`]].map(([time,title,detail]) => <div className="relative" key={title}><i className="absolute -left-[25px] top-1 h-2 w-2 rounded-full bg-brand"/><p className="text-sm font-semibold">{title}</p><p className="mt-1 text-xs text-slate-500">{detail}</p><p className="mt-1.5 text-[11px] text-slate-400">{time}</p></div>)}</div></section></div></aside></div>; }

function JobModal({ onClose, onSubmit, error }: { onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void; error: string }) { return <div className="fixed inset-0 z-[60] grid place-items-center bg-ink/30 p-4" onClick={onClose}><form onSubmit={onSubmit} className="w-full max-w-lg rounded-2xl bg-white shadow-2xl" onClick={(e) => e.stopPropagation()}><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-semibold">Create a new job</h2><p className="mt-0.5 text-xs text-slate-500">Start with the essentials. You can refine it later.</p></div><button type="button" onClick={onClose} aria-label="Close"><X size={19}/></button></div><div className="space-y-4 p-5"><label className="block text-sm font-semibold">Job title<input name="title" placeholder="e.g. Senior Frontend Engineer" className="mt-1.5 h-10 w-full rounded-lg border px-3 text-sm font-normal outline-none focus:border-brand focus:ring-2 focus:ring-blue-100"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold">Department<input name="department" placeholder="Engineering" className="mt-1.5 h-10 w-full rounded-lg border px-3 text-sm font-normal outline-none focus:border-brand focus:ring-2 focus:ring-blue-100"/></label><label className="block text-sm font-semibold">Location<input name="location" placeholder="Remote / city" className="mt-1.5 h-10 w-full rounded-lg border px-3 text-sm font-normal outline-none focus:border-brand focus:ring-2 focus:ring-blue-100"/></label></div><label className="block text-sm font-semibold">Employment type<select className="mt-1.5 h-10 w-full rounded-lg border bg-white px-3 text-sm font-normal outline-none focus:border-brand"><option>Full-time</option><option>Contract</option><option>Part-time</option></select></label>{error && <p role="alert" className="rounded-lg bg-rose-50 p-3 text-sm font-medium text-rose-700">{error}</p>}</div><div className="flex justify-end gap-3 border-t px-5 py-4"><button type="button" onClick={onClose} className="h-10 rounded-xl px-4 text-sm font-semibold hover:bg-slate-50">Cancel</button><button type="submit" className="h-10 rounded-xl bg-brand px-4 text-sm font-semibold text-white hover:bg-blue-700">Save as draft</button></div></form></div>; }
